void add_vectors(i64[n] a, i64[n] b, mut i64[n] dest, u64 n) {
    for u64 i <- 0 .. n {
        dest[i] <- a[i] + b[i]
    }
}

i32 main() {
    /* pas encore de création de tableaux */
    return 0
}